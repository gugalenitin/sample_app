<% provide(:title, 'All Users') %>
<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
	<!-- <% @users.each do |u| %>
		# Renders _user.html.erb file.
		<%= render 'user', user: u %>
					OR
		<%= render u %> 
	<% end %> -->
	<%= render @users %>
</ul>

<%= will_paginate %>