->heroku login
->cd C:\Users\yourusername\.ssh
->ssh-keygen -t rsa -f id_rsa

if asks any passphrase don't use blank, fill with a passphrase,but not forget it.

->heroku keys:add "id_rsa.pub"
->git push heroku (Enter the passphrase)
